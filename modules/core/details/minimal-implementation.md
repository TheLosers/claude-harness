# 지금 필요한 함수만 만든다

> **지금 호출되지 않는 함수를 미리 만들지 않는다.**

나중에 필요할 것 같다는 이유로 조회·검색·분류 함수를 함께 만들지 않는다.
호출하는 곳이 없는 함수도 테스트와 변경 비용을 만든다.

1. 사용자 요청에 필요한 기능을 적는다.
2. 필요한 API와 서비스 메서드, 실제 호출 순서를 확인한다.
3. 그 호출에 필요한 함수만 구현한다.

예를 들어 도구 이름으로 조회하는 기능이 필요하다면 이름 조회를 만든다.
분류별 검색은 실제 요구가 생겼을 때 추가한다.
